# 진행 계획

> [ARCHITECTURE.md](ARCHITECTURE.md)의 지침을 **단계별로** 실행하기 위한 계획입니다.
> 최종 지향점으로 한 번에 가지 않고, 각 단계가 **그 자체로 동작하는 산출물**을
> 내도록 쪼갰습니다.

## 원칙

1. **각 단계는 쓸 수 있는 것을 남긴다.** 다음 단계가 없어도 그 자체로 동작해야 한다.
2. **측정할 수 없으면 진행하지 않는다.** 단계마다 완료 기준을 숫자로 둔다.
3. **데이터를 직접 만들 수 있는 것부터 한다.** 외부 데이터 의존도가 낮은 순서.
4. **어려운 결정은 뒤로 미루되, 그 결정을 막는 구조는 만들지 않는다.**

---

## Phase 0 — 정리 (현재 단계)

**목표:** 남은 코드를 최종 지향점에 연결되는 것만 남기고 정리한다.

지금 저장소에는 발표용으로 급조된 경로와 실제로 쓸 경로가 섞여 있다.
여기서 정리하지 않으면 이후 모든 단계에서 "이건 왜 있지"를 반복하게 된다.

**할 일**
- 죽은 경로 제거 — 서버 프레임 전송 구조, SocketIO 서버측 웹캠, 임시 채점 로직
- 유지할 것과 버릴 것을 명시적으로 결정하고 기록
- 파이프라인 계약 정의
  - 좌우 반전 정책을 **단일 상수**로
  - 특징 스키마(차원, 순서, 좌표계)를 한 곳에서 정의
  - 학습·추론이 같은 정의를 참조하도록
- 프론트엔드 라우팅과 페이지 역할 재정의
- 빌드가 통과하는 상태 확보 (`npm install` → `npm run build`)

**완료 기준**
- 빌드 성공
- 남은 코드 중 현재 목표와 연결되지 않는 경로 **0개**
- 특징 스키마와 반전 정책이 **한 파일에서만** 정의됨

**산출물:** 깨끗한 뼈대. 이후 단계가 얹힐 토대.

---

## Phase 1 — 지문자 인식 (자음·모음 전체)

**목표:** 브라우저 단독으로 동작하는 지문자 인식기.

**왜 이 순서인가**
- **데이터를 직접 만들 수 있다.** 웹캠으로 녹화하면 끝이라 외부 데이터가 필요 없다.
- **정지 손모양이라 문제가 쉽다.** 궤적·구간 분할 같은 어려운 문제를 피해 간다.
- **그 자체로 쓸모가 있다.** 지문자 입력기만으로도 이름·고유명사를 전달할 수 있다.
- **뒤 단계의 안전망이 된다.** 사전에 없는 단어를 지문자로 떨어뜨리는 설계(지침 8)의 전제.

**할 일**
- 데이터 수집 도구 (브라우저에서 녹화 → 키포인트 저장)
- 특징: MediaPipe 손 **3D**, 손 방향 포함
- **idle 클래스 포함** (지침 2)
- **좌우 반전 증강** (지침 6) — 왼손잡이 대응
- hold 검출: N프레임 이상 안정 시 확정 (지침 1)
- 자모 → 한글 음절 조합 (초성·중성·종성 결합)
- 브라우저 추론 (ONNX Runtime Web)

**완료 기준**
- 자음·모음 전체 + idle 분류
- 오른손·왼손 모두 동작
- 확정까지 지연 **0.5초 이내**
- 무동작 상태에서 **헛출력 없음**
- 자모를 이어서 한글 단어를 입력할 수 있음

**산출물:** 실제로 쓸 수 있는 지문자 입력기.

---

## Phase 2 — 구간 분할

**목표:** "지금 수어를 하는 중인가"를 학습 기반으로 판별한다.

Phase 1의 idle 클래스를 일반화하는 단계. **단어로 넘어가기 위한 필수 전제**이고,
이게 없으면 이후 어떤 모델을 만들어도 자막이 헛단어로 뒤덮인다.

**할 일**
- 동작 시작·끝 검출 모델
- 전이 동작(단어와 단어 사이)을 idle로 학습
- 확정 로직: 신뢰도 임계값 + 시간축 스무딩

**완료 기준**
- 무동작 1분간 헛출력 **0회**
- 동작 시작 검출 지연 **0.3초 이내**
- 연속 동작에서 경계 검출 재현율 목표치 달성

---

## Phase 3 — 소규모 단어 세트 (20~50개)

**목표:** 단어 단위 인식의 구조를 검증한다. 어휘 규모가 아니라 **구조**가 목적.

**할 일**
- AI Hub 데이터 재확보 후 **MediaPipe 3D로 재추출** (OpenPose 특징 사용 금지)
- 학습 시 **무작위 구간 크롭** (지침 3)
- 길이 불변 구조 (CTC 또는 어텐션 풀링)
- **스트리밍 추론** — 은닉 상태를 이어가 지연을 줄인다 (지침 10)
- Phase 2의 구간 분할과 연결

**완료 기준**
- 단어 세트 내 목표 정확도 달성
- 발화 종료 후 자막까지 **1초 이내**
- 속도 편차(빠르게/느리게)에 대한 정확도 하락이 허용 범위 내

**주의:** 여기서 정확도가 안 나오면 어휘를 늘려도 소용없다. 규모를 늘리기 전에 구조를 고친다.

---

## Phase 4 — 어휘 확장

**목표:** 재학습 없이 단어를 추가할 수 있는 구조로 전환한다.

**할 일**
- 고정 softmax → **임베딩 + 최근접 이웃** (지침 8)
- 미지 단어 → **지문자 폴백** (Phase 1과 연결)
- 클래스 불균형 대응

**완료 기준**
- 새 단어 추가 시 **전체 재학습 불필요**
- 단어 추가 후 기존 단어 정확도 하락이 허용 범위 내
- 사전에 없는 수어에 대해 엉뚱한 단어 대신 지문자 또는 "모름"을 출력

---

## Phase 5 — 문장화

**목표:** gloss 나열을 자연스러운 한국어로 만든다.

**할 일**
- `gloss 시퀀스 → 한국어` 변환 단계 (지침 9)
- 인식 정확도와 문장 품질을 **분리해서** 측정

**완료 기준:** 인식 결과가 같을 때 문장 품질만 독립적으로 평가 가능

---

## Phase 6 — 비수지 신호

**목표:** 표정·입모양을 문법 요소로 반영한다 (지침 5).

의문·부정·정도 구분. 앞 단계가 안정된 뒤에 착수한다.

---

## 단계 간 의존 관계

```
Phase 0 (정리)
   └─> Phase 1 (지문자) ──────────────┐
          └─> Phase 2 (구간 분할)      │
                 └─> Phase 3 (단어)    │
                        └─> Phase 4 (어휘 확장) <┘  ← 지문자 폴백으로 연결
                               └─> Phase 5 (문장화)
                                      └─> Phase 6 (비수지)
```

Phase 1과 4가 **지문자 폴백**으로 이어진다는 점이 이 계획의 핵심이다.
지문자를 먼저 완성해두면, 단어 인식이 실패해도 시스템이 침묵하지 않는다.

---

## 지금 정하지 않아도 되는 것

- 최종 어휘 규모 — Phase 3에서 구조를 검증한 뒤 결정
- 모델 아키텍처 세부 — Phase 1의 측정 결과를 보고 결정
- 배포 방식 — 브라우저 단독이므로 정적 호스팅으로 충분, 나중에 결정

## 지금 정해야 하는 것

- 좌우 반전 정책 (Phase 0)
- 특징 스키마와 좌표계 (Phase 0)
- 평가 지표와 측정 방법 (Phase 0~1)
