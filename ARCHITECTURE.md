# 설계 지침 — 최종 지향점 기준

> 이 문서는 **완성된 손짓**을 기준으로 쓴 설계 판단 기록입니다.
> 지금 남아 있는 코드의 앞뒤가 맞는지가 아니라,
> **어떤 구조를 고르면 무엇이 인식되지 않는가**를 정리했습니다.
>
> 각 항목은 `구조 선택 → 그 결과 나타나는 인식 실패`의 형태로 적었습니다.

## 최종 지향점

**웹캠 앞에서 한국수어를 하면 실시간으로 한국어 자막이 나오는 웹서비스.**

| 항목 | 목표 |
|---|---|
| 지문자 | 자음·모음 **전체** |
| 단어 | 가능한 한 많이 (수백~수천 규모를 상정) |
| 지연 | 대화가 가능한 수준 (1초 내외) |
| 실행 위치 | 브라우저 단독 |
| 사용자 | 오른손잡이·왼손잡이 모두 |

---

## 1. 지문자와 단어는 분리한다

지문자는 **정지 손모양**, 단어는 **궤적**이다. 성질이 다르다.

**하나의 N-way softmax에 섞으면:** 시퀀스 모델은 단어를 맞히려 움직임에 가중치를 둔다.
지문자는 움직임이 없으므로 불리해진다. 반대로 단어 수어 중간에 스쳐가는 손모양이
ㄱ·ㅅ·ㅇ과 닮으면 그때마다 자모가 튀어나온다.

> **증상:** 단어 수어 중 자모가 계속 섞여 나온다.
> 클래스가 늘어날수록 지문자 정확도가 먼저 무너진다.

**지침:** 지문자 모델과 단어 모델을 분리하고, 앞단에서 모드를 판별한다.
지문자는 "N프레임 이상 손모양이 안정적으로 유지됨(hold)"을 확정 조건으로 둔다.

## 2. "아무것도 아님" 클래스를 반드시 둔다

softmax 합은 항상 1이다. idle 클래스가 없으면 **모델이 침묵할 수단이 없다.**

> **증상:** 손을 내리는 동작, 단어 사이 전이 동작, 머리 긁는 동작이 전부
> 어떤 단어로 출력된다. 어휘가 커질수록 헛단어 비율이 올라간다.

**지침:** 학습 데이터에 idle 구간을 클래스로 포함시킨다.
그 위에 신뢰도 임계값과 시간축 스무딩(연속 K프레임 동일 예측 시 확정)을 얹는다.
움직임 임계값 같은 휴리스틱은 임시방편일 뿐 학습된 구간 분할을 대체하지 못한다.

## 3. 고정 길이 창에 의존하지 않는다

"클립 앞에서부터 N프레임, 뒤는 0으로 패딩"으로 학습하면
모델은 **창 안에서의 위치**를 단서로 학습한다. 실시간 스트림에는 클립 경계가 없다.

> **증상:** 같은 단어라도 빠르게 하면 못 맞힌다.
> 동작 시작점이 창 중간에 걸리면 실패한다. 단어 길이 편차가 클수록 심해진다.

**지침:** 학습 시 **무작위 구간을 잘라** 정렬 의존성을 없앤다.
길이 불변 구조(CTC, 어텐션 풀링)를 우선한다.

## 4. 3D를 쓴다

한국수어는 손이 몸에서 앞으로 나오는지 붙는지로 의미가 갈리는 쌍이 많다.
2D 투영에서는 이 차이가 사라진다.

> **증상:** 어휘 100개까지는 버티다가 수백 개로 늘리면
> **손모양이 같고 앞뒤 움직임만 다른 단어들**이 서로 먹기 시작한다.
> 정확도가 클래스 수에 비선형으로 떨어진다.

**지침:** MediaPipe가 주는 z를 버리지 않는다. 손 방향(orientation)도 명시적 특징으로 넣는다.

## 5. 표정을 특징으로 다룬다

한국수어에서 표정과 입모양은 장식이 아니라 **의문·부정·정도를 나타내는 문법 요소**다.

> **증상:** "가다"와 "안 가다", 평서문과 의문문이 같은 단어로 인식된다.
> 손만 봐서는 원리적으로 구분이 불가능하다.

**지침:** 얼굴을 별도 채널로 모델링하거나, 최소한 눈썹·입 주변 랜드마크를 따로 특징화한다.

## 6. 좌우 반전은 버그가 아니라 정책이다

파일마다 각자 판단하면 학습과 추론이 어긋난다.
더 중요한 것은 **좌우 반전 증강이 없으면 왼손잡이를 통째로 인식하지 못한다**는 점이다.

> **증상:** 왼손 서명자에게 전혀 동작하지 않는다.

**지침:**
- 학습에 **좌우 반전 증강을 넣는다.** 왼손잡이 대응과 반전 불일치가 동시에 해결된다.
- 반전 여부를 **파이프라인 전체가 참조하는 단일 상수**로 못박는다.
- 화면에 보여주는 미러링(UX)과 모델에 넣는 영상은 **별개**임을 코드에 명시한다.

## 7. 추론은 브라우저에서 한다

프레임을 서버로 보내는 구조는 실시간 단어 인식과 양립하지 않는다.

> **증상:** 전송 비용 때문에 프레임 레이트를 낮추면 궤적이 사라져
> 동작 기반 단어 인식이 원리적으로 불가능해진다.
> 서버에서 MediaPipe를 돌리면 동시 사용자가 늘 때 줄을 선다.

**지침:** MediaPipe와 모델 추론을 모두 브라우저에서 수행한다.
LSTM 계열 모델은 수 MB 수준이라 ONNX Runtime Web으로 충분하다.
영상이 서버로 나가지 않으므로 **개인정보 측면에서도 유리하다.**

## 8. 어휘 확장은 고정 softmax로 하지 않는다

클래스를 늘릴 때마다 전체 재학습이 필요하고, 데이터가 적은 단어는 빈출 단어에 흡수된다.

> **증상:** 단어를 추가할수록 기존 단어 정확도가 함께 떨어진다.
> 희귀 단어는 사실상 출력되지 않는다.

**지침:** **임베딩 + 최근접 이웃** 방식을 쓴다. 재학습 없이 단어를 추가할 수 있다.
그리고 결정적으로 — **사전에 없는 단어는 지문자로 떨어뜨린다.**
엉뚱한 단어를 내놓는 대신 자모로 풀어 쓰는 것이 어휘 확장 전략의 핵심이다.

## 9. 단어 나열은 한국어 문장이 아니다

한국수어는 어순이 다르고 조사가 없다.

> **증상:** 인식이 완벽해도 출력은 "나 학교 가다"가 된다. 자막으로 쓸 수 없다.

**지침:** `gloss 시퀀스 → 자연스러운 한국어` 변환을 **처음부터 별도 단계로** 설계에 넣는다.
인식 정확도와 문장 품질을 분리해서 측정할 수 있어야 한다.

## 10. 지연 시간을 설계 제약으로 잡는다

N프레임 창이 다 차야 첫 출력이 나오는 구조는 그만큼 지연된다.
150프레임 @30fps면 5초다.

> **증상:** 수어를 마치고 몇 초 뒤에 자막이 뜬다. 실시간 소통 도구로 쓸 수 없다.

**지침:** 은닉 상태를 이어가는 **스트리밍 추론**이나 짧은 창 + CTC를 쓴다.
지연 목표를 숫자로 정하고 회귀 테스트에 포함시킨다.

---

## 데이터에 대한 결정

**키포인트는 처음부터 MediaPipe로 추출한다.**

OpenPose 특징으로 학습해두면 브라우저에서 재현할 수 없다.
OpenPose는 모든 키포인트에 confidence를 주지만 **MediaPipe는 얼굴·손 랜드마크에
confidence를 주지 않는다.** 학습 때 실측값이 들어간 자리에 추론 때는 상수가 들어가고,
이 간극은 코드로 메울 수 없다.

추론 환경이 브라우저라면 학습 특징도 브라우저에서 얻을 수 있는 것만으로 구성해야 한다.

---

## 우선순위

이 중 **1(지문자/단어 분리)**, **2(idle 클래스와 구간 분할)**, **8(임베딩 기반 확장)**이
"최대한 많은 단어"를 지탱하는 뼈대다. 나머지는 그 위에 붙는다.

특히 **2가 없으면 나머지를 아무리 잘 만들어도 자막이 헛단어로 뒤덮여
체감 품질이 나오지 않는다.**

단계별 실행 계획은 [ROADMAP.md](ROADMAP.md) 참고.
